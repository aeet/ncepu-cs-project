// Code generated by ent, DO NOT EDIT.

package domain

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/devcui/ncepu-cs-project/domain/class"
	"github.com/devcui/ncepu-cs-project/domain/classleader"
	"github.com/devcui/ncepu-cs-project/domain/student"
)

// ClassLeader is the model entity for the ClassLeader schema.
type ClassLeader struct {
	config
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ClassLeaderQuery when eager-loading is set.
	Edges ClassLeaderEdges `json:"edges"`
}

// ClassLeaderEdges holds the relations/edges for other nodes in the graph.
type ClassLeaderEdges struct {
	// Class holds the value of the class edge.
	Class *Class `json:"class,omitempty"`
	// Student holds the value of the student edge.
	Student *Student `json:"student,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ClassOrErr returns the Class value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ClassLeaderEdges) ClassOrErr() (*Class, error) {
	if e.loadedTypes[0] {
		if e.Class == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: class.Label}
		}
		return e.Class, nil
	}
	return nil, &NotLoadedError{edge: "class"}
}

// StudentOrErr returns the Student value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ClassLeaderEdges) StudentOrErr() (*Student, error) {
	if e.loadedTypes[1] {
		if e.Student == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: student.Label}
		}
		return e.Student, nil
	}
	return nil, &NotLoadedError{edge: "student"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ClassLeader) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case classleader.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type ClassLeader", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ClassLeader fields.
func (cl *ClassLeader) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case classleader.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			cl.ID = int(value.Int64)
		}
	}
	return nil
}

// QueryClass queries the "class" edge of the ClassLeader entity.
func (cl *ClassLeader) QueryClass() *ClassQuery {
	return NewClassLeaderClient(cl.config).QueryClass(cl)
}

// QueryStudent queries the "student" edge of the ClassLeader entity.
func (cl *ClassLeader) QueryStudent() *StudentQuery {
	return NewClassLeaderClient(cl.config).QueryStudent(cl)
}

// Update returns a builder for updating this ClassLeader.
// Note that you need to call ClassLeader.Unwrap() before calling this method if this ClassLeader
// was returned from a transaction, and the transaction was committed or rolled back.
func (cl *ClassLeader) Update() *ClassLeaderUpdateOne {
	return NewClassLeaderClient(cl.config).UpdateOne(cl)
}

// Unwrap unwraps the ClassLeader entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (cl *ClassLeader) Unwrap() *ClassLeader {
	_tx, ok := cl.config.driver.(*txDriver)
	if !ok {
		panic("domain: ClassLeader is not a transactional entity")
	}
	cl.config.driver = _tx.drv
	return cl
}

// String implements the fmt.Stringer.
func (cl *ClassLeader) String() string {
	var builder strings.Builder
	builder.WriteString("ClassLeader(")
	builder.WriteString(fmt.Sprintf("id=%v", cl.ID))
	builder.WriteByte(')')
	return builder.String()
}

// ClassLeaders is a parsable slice of ClassLeader.
type ClassLeaders []*ClassLeader
