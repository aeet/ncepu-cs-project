// Code generated by ent, DO NOT EDIT.

package domain

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/devcui/ncepu-cs-project/domain/educationlevel"
	"github.com/devcui/ncepu-cs-project/domain/student"
)

// EducationLevel is the model entity for the EducationLevel schema.
type EducationLevel struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 培养层次名称
	Name string `json:"name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the EducationLevelQuery when eager-loading is set.
	Edges                   EducationLevelEdges `json:"edges"`
	education_level_student *int
}

// EducationLevelEdges holds the relations/edges for other nodes in the graph.
type EducationLevelEdges struct {
	// Student holds the value of the student edge.
	Student *Student `json:"student,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// StudentOrErr returns the Student value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e EducationLevelEdges) StudentOrErr() (*Student, error) {
	if e.loadedTypes[0] {
		if e.Student == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: student.Label}
		}
		return e.Student, nil
	}
	return nil, &NotLoadedError{edge: "student"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EducationLevel) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case educationlevel.FieldID:
			values[i] = new(sql.NullInt64)
		case educationlevel.FieldName:
			values[i] = new(sql.NullString)
		case educationlevel.ForeignKeys[0]: // education_level_student
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type EducationLevel", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EducationLevel fields.
func (el *EducationLevel) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case educationlevel.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			el.ID = int(value.Int64)
		case educationlevel.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				el.Name = value.String
			}
		case educationlevel.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field education_level_student", value)
			} else if value.Valid {
				el.education_level_student = new(int)
				*el.education_level_student = int(value.Int64)
			}
		}
	}
	return nil
}

// QueryStudent queries the "student" edge of the EducationLevel entity.
func (el *EducationLevel) QueryStudent() *StudentQuery {
	return NewEducationLevelClient(el.config).QueryStudent(el)
}

// Update returns a builder for updating this EducationLevel.
// Note that you need to call EducationLevel.Unwrap() before calling this method if this EducationLevel
// was returned from a transaction, and the transaction was committed or rolled back.
func (el *EducationLevel) Update() *EducationLevelUpdateOne {
	return NewEducationLevelClient(el.config).UpdateOne(el)
}

// Unwrap unwraps the EducationLevel entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (el *EducationLevel) Unwrap() *EducationLevel {
	_tx, ok := el.config.driver.(*txDriver)
	if !ok {
		panic("domain: EducationLevel is not a transactional entity")
	}
	el.config.driver = _tx.drv
	return el
}

// String implements the fmt.Stringer.
func (el *EducationLevel) String() string {
	var builder strings.Builder
	builder.WriteString("EducationLevel(")
	builder.WriteString(fmt.Sprintf("id=%v, ", el.ID))
	builder.WriteString("name=")
	builder.WriteString(el.Name)
	builder.WriteByte(')')
	return builder.String()
}

// EducationLevels is a parsable slice of EducationLevel.
type EducationLevels []*EducationLevel
