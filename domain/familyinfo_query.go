// Code generated by ent, DO NOT EDIT.

package domain

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/devcui/ncepu-cs-project/domain/familyinfo"
	"github.com/devcui/ncepu-cs-project/domain/predicate"
	"github.com/devcui/ncepu-cs-project/domain/student"
)

// FamilyInfoQuery is the builder for querying FamilyInfo entities.
type FamilyInfoQuery struct {
	config
	ctx         *QueryContext
	order       []OrderFunc
	inters      []Interceptor
	predicates  []predicate.FamilyInfo
	withStudent *StudentQuery
	withFKs     bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FamilyInfoQuery builder.
func (fiq *FamilyInfoQuery) Where(ps ...predicate.FamilyInfo) *FamilyInfoQuery {
	fiq.predicates = append(fiq.predicates, ps...)
	return fiq
}

// Limit the number of records to be returned by this query.
func (fiq *FamilyInfoQuery) Limit(limit int) *FamilyInfoQuery {
	fiq.ctx.Limit = &limit
	return fiq
}

// Offset to start from.
func (fiq *FamilyInfoQuery) Offset(offset int) *FamilyInfoQuery {
	fiq.ctx.Offset = &offset
	return fiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fiq *FamilyInfoQuery) Unique(unique bool) *FamilyInfoQuery {
	fiq.ctx.Unique = &unique
	return fiq
}

// Order specifies how the records should be ordered.
func (fiq *FamilyInfoQuery) Order(o ...OrderFunc) *FamilyInfoQuery {
	fiq.order = append(fiq.order, o...)
	return fiq
}

// QueryStudent chains the current query on the "student" edge.
func (fiq *FamilyInfoQuery) QueryStudent() *StudentQuery {
	query := (&StudentClient{config: fiq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fiq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(familyinfo.Table, familyinfo.FieldID, selector),
			sqlgraph.To(student.Table, student.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, familyinfo.StudentTable, familyinfo.StudentColumn),
		)
		fromU = sqlgraph.SetNeighbors(fiq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first FamilyInfo entity from the query.
// Returns a *NotFoundError when no FamilyInfo was found.
func (fiq *FamilyInfoQuery) First(ctx context.Context) (*FamilyInfo, error) {
	nodes, err := fiq.Limit(1).All(setContextOp(ctx, fiq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{familyinfo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fiq *FamilyInfoQuery) FirstX(ctx context.Context) *FamilyInfo {
	node, err := fiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FamilyInfo ID from the query.
// Returns a *NotFoundError when no FamilyInfo ID was found.
func (fiq *FamilyInfoQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fiq.Limit(1).IDs(setContextOp(ctx, fiq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{familyinfo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fiq *FamilyInfoQuery) FirstIDX(ctx context.Context) int {
	id, err := fiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FamilyInfo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FamilyInfo entity is found.
// Returns a *NotFoundError when no FamilyInfo entities are found.
func (fiq *FamilyInfoQuery) Only(ctx context.Context) (*FamilyInfo, error) {
	nodes, err := fiq.Limit(2).All(setContextOp(ctx, fiq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{familyinfo.Label}
	default:
		return nil, &NotSingularError{familyinfo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fiq *FamilyInfoQuery) OnlyX(ctx context.Context) *FamilyInfo {
	node, err := fiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FamilyInfo ID in the query.
// Returns a *NotSingularError when more than one FamilyInfo ID is found.
// Returns a *NotFoundError when no entities are found.
func (fiq *FamilyInfoQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fiq.Limit(2).IDs(setContextOp(ctx, fiq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{familyinfo.Label}
	default:
		err = &NotSingularError{familyinfo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fiq *FamilyInfoQuery) OnlyIDX(ctx context.Context) int {
	id, err := fiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FamilyInfos.
func (fiq *FamilyInfoQuery) All(ctx context.Context) ([]*FamilyInfo, error) {
	ctx = setContextOp(ctx, fiq.ctx, "All")
	if err := fiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FamilyInfo, *FamilyInfoQuery]()
	return withInterceptors[[]*FamilyInfo](ctx, fiq, qr, fiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fiq *FamilyInfoQuery) AllX(ctx context.Context) []*FamilyInfo {
	nodes, err := fiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FamilyInfo IDs.
func (fiq *FamilyInfoQuery) IDs(ctx context.Context) (ids []int, err error) {
	if fiq.ctx.Unique == nil && fiq.path != nil {
		fiq.Unique(true)
	}
	ctx = setContextOp(ctx, fiq.ctx, "IDs")
	if err = fiq.Select(familyinfo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fiq *FamilyInfoQuery) IDsX(ctx context.Context) []int {
	ids, err := fiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fiq *FamilyInfoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fiq.ctx, "Count")
	if err := fiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fiq, querierCount[*FamilyInfoQuery](), fiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fiq *FamilyInfoQuery) CountX(ctx context.Context) int {
	count, err := fiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fiq *FamilyInfoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fiq.ctx, "Exist")
	switch _, err := fiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("domain: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fiq *FamilyInfoQuery) ExistX(ctx context.Context) bool {
	exist, err := fiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FamilyInfoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fiq *FamilyInfoQuery) Clone() *FamilyInfoQuery {
	if fiq == nil {
		return nil
	}
	return &FamilyInfoQuery{
		config:      fiq.config,
		ctx:         fiq.ctx.Clone(),
		order:       append([]OrderFunc{}, fiq.order...),
		inters:      append([]Interceptor{}, fiq.inters...),
		predicates:  append([]predicate.FamilyInfo{}, fiq.predicates...),
		withStudent: fiq.withStudent.Clone(),
		// clone intermediate query.
		sql:  fiq.sql.Clone(),
		path: fiq.path,
	}
}

// WithStudent tells the query-builder to eager-load the nodes that are connected to
// the "student" edge. The optional arguments are used to configure the query builder of the edge.
func (fiq *FamilyInfoQuery) WithStudent(opts ...func(*StudentQuery)) *FamilyInfoQuery {
	query := (&StudentClient{config: fiq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fiq.withStudent = query
	return fiq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (fiq *FamilyInfoQuery) GroupBy(field string, fields ...string) *FamilyInfoGroupBy {
	fiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FamilyInfoGroupBy{build: fiq}
	grbuild.flds = &fiq.ctx.Fields
	grbuild.label = familyinfo.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (fiq *FamilyInfoQuery) Select(fields ...string) *FamilyInfoSelect {
	fiq.ctx.Fields = append(fiq.ctx.Fields, fields...)
	sbuild := &FamilyInfoSelect{FamilyInfoQuery: fiq}
	sbuild.label = familyinfo.Label
	sbuild.flds, sbuild.scan = &fiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FamilyInfoSelect configured with the given aggregations.
func (fiq *FamilyInfoQuery) Aggregate(fns ...AggregateFunc) *FamilyInfoSelect {
	return fiq.Select().Aggregate(fns...)
}

func (fiq *FamilyInfoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fiq.inters {
		if inter == nil {
			return fmt.Errorf("domain: uninitialized interceptor (forgotten import domain/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fiq); err != nil {
				return err
			}
		}
	}
	for _, f := range fiq.ctx.Fields {
		if !familyinfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("domain: invalid field %q for query", f)}
		}
	}
	if fiq.path != nil {
		prev, err := fiq.path(ctx)
		if err != nil {
			return err
		}
		fiq.sql = prev
	}
	return nil
}

func (fiq *FamilyInfoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FamilyInfo, error) {
	var (
		nodes       = []*FamilyInfo{}
		withFKs     = fiq.withFKs
		_spec       = fiq.querySpec()
		loadedTypes = [1]bool{
			fiq.withStudent != nil,
		}
	)
	if fiq.withStudent != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, familyinfo.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FamilyInfo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FamilyInfo{config: fiq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := fiq.withStudent; query != nil {
		if err := fiq.loadStudent(ctx, query, nodes, nil,
			func(n *FamilyInfo, e *Student) { n.Edges.Student = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (fiq *FamilyInfoQuery) loadStudent(ctx context.Context, query *StudentQuery, nodes []*FamilyInfo, init func(*FamilyInfo), assign func(*FamilyInfo, *Student)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*FamilyInfo)
	for i := range nodes {
		if nodes[i].family_info_student == nil {
			continue
		}
		fk := *nodes[i].family_info_student
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(student.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "family_info_student" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (fiq *FamilyInfoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fiq.querySpec()
	_spec.Node.Columns = fiq.ctx.Fields
	if len(fiq.ctx.Fields) > 0 {
		_spec.Unique = fiq.ctx.Unique != nil && *fiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fiq.driver, _spec)
}

func (fiq *FamilyInfoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(familyinfo.Table, familyinfo.Columns, sqlgraph.NewFieldSpec(familyinfo.FieldID, field.TypeInt))
	_spec.From = fiq.sql
	if unique := fiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fiq.path != nil {
		_spec.Unique = true
	}
	if fields := fiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, familyinfo.FieldID)
		for i := range fields {
			if fields[i] != familyinfo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fiq *FamilyInfoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fiq.driver.Dialect())
	t1 := builder.Table(familyinfo.Table)
	columns := fiq.ctx.Fields
	if len(columns) == 0 {
		columns = familyinfo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fiq.sql != nil {
		selector = fiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fiq.ctx.Unique != nil && *fiq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fiq.predicates {
		p(selector)
	}
	for _, p := range fiq.order {
		p(selector)
	}
	if offset := fiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FamilyInfoGroupBy is the group-by builder for FamilyInfo entities.
type FamilyInfoGroupBy struct {
	selector
	build *FamilyInfoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (figb *FamilyInfoGroupBy) Aggregate(fns ...AggregateFunc) *FamilyInfoGroupBy {
	figb.fns = append(figb.fns, fns...)
	return figb
}

// Scan applies the selector query and scans the result into the given value.
func (figb *FamilyInfoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, figb.build.ctx, "GroupBy")
	if err := figb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FamilyInfoQuery, *FamilyInfoGroupBy](ctx, figb.build, figb, figb.build.inters, v)
}

func (figb *FamilyInfoGroupBy) sqlScan(ctx context.Context, root *FamilyInfoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(figb.fns))
	for _, fn := range figb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*figb.flds)+len(figb.fns))
		for _, f := range *figb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*figb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := figb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FamilyInfoSelect is the builder for selecting fields of FamilyInfo entities.
type FamilyInfoSelect struct {
	*FamilyInfoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fis *FamilyInfoSelect) Aggregate(fns ...AggregateFunc) *FamilyInfoSelect {
	fis.fns = append(fis.fns, fns...)
	return fis
}

// Scan applies the selector query and scans the result into the given value.
func (fis *FamilyInfoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fis.ctx, "Select")
	if err := fis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FamilyInfoQuery, *FamilyInfoSelect](ctx, fis.FamilyInfoQuery, fis, fis.inters, v)
}

func (fis *FamilyInfoSelect) sqlScan(ctx context.Context, root *FamilyInfoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fis.fns))
	for _, fn := range fis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
